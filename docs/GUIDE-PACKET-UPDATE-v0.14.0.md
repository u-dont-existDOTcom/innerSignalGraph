# Inner Signal v0.14.0 — Guide Packet Update

## Implemented

- `inner-signal-guide-packet-v1` packet contract.
- Safe ZIP parsing and path/CRC/size/duplicate/symlink/encryption rejection.
- Deterministic manifest/checksum/schema/source-map/section/graph/provenance/certainty/regression validation.
- Candidate staging without changing the active guide graph.
- Opus source-role compilation, Codex independent audit, and conditional Fable escalation.
- Exact source and section-level diff.
- Behavioral graph diff and concise owner decision cards.
- Reverse graph-to-guide quality audit.
- Atomic install, exact rollback, installed-packet export, and export/re-import identity.
- Guides web screen and API endpoints.
- Packet processing status in the always-visible Overall Development supervisor.
- Installed packet/guide/graph identity in runtime health and decision ledgers.
- Candidate fixture from the supplied inner-child and somatic HTML sources.

## Deterministically tested

- Valid import; tamper and zip-slip rejection; missing-member rejection.
- Same/older version rejection.
- Stale source/graph/source-map/sections rejection.
- Provenance enforcement and owner-amendment allowance.
- Exact behavioral route/question/priority/deferred changes.
- Substantive decision-card gating and routine metadata auto-stage behavior.
- Atomic install/rollback and export/re-import identity.
- A001/H001 non-regression and private-data exclusion.
- Reverse guide-quality findings and supervisor visibility.

## Pending owner decisions

The r01 packet is staged as a candidate only. Its four substantive decisions are shown in the Guides screen and must be approved individually. Production therapy policy remains on the previously installed guide/graph bundle until then.

## Source limitation

The two article-family ZIPs named in the handoff were not present in the conversation files, active filesystem, or File Library search. The exact candidate HTML and editor-body hashes match the handoff. The packet records the family ZIPs as unavailable and preserves their expected hashes rather than fabricating the missing project-state/ledger files.

## Model-review limitation in this build environment

The current build container had no `claude` or `codex` executable, so no real subscription-backed model call was claimed. Structured fake-provider tests cover the full Opus → Codex → conditional Fable path. On Joel's machine, the one-command autopilot performs the real model compilation/review and persists it into the candidate packet before owner installation.
