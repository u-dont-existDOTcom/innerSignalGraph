# Turn-bound longitudinal continuity — implementation entrypoint

Status: architecture and isolated reference delivered; application integration and native-host proof remain OPEN. No real private case read or changed, no paid inference, no deployment, no stable promotion.

## Owner outcome

Continuing an established case should not require the operator to repeatedly remind the reasoner of already recorded history. Develop the architecture in the reasoning chat and give Work the remaining bounded implementation, with no silent change to the preferred native ChatGPT model, billing route or user-facing plugin.

## Exact handoff package

Delivered attachment: `InnerSignal-Continuity-Architecture-and-Work-20260917.zip`.

SHA-256: `421a484b7ba00bab5d5594ea94df56dabe2c1133866f66df885ed191e15c61fa`.

Manifest SHA-256: `4bbbb3f65a0c5e05074a1c46537aac0dba0251466d71429e483428f9ecaddd75`.

The package has 24 files: the full architecture, interfaces, runnable Work instruction, verification plan, primary-source register, proposed native instruction, transport schemas, isolated JavaScript reference and tests, twelve synthetic writer/evaluator cases, and actual verification evidence. This repository entrypoint is the recovery pointer and decision record, not a claim that the whole attachment has already been imported into Git.

Work starts with `03-WORK-INSTRUCTION.md` and the whole package, then imports the reviewed artifacts into this task directory. No private patient payload belongs in Git. If the attachment is absent, report that exact artifact-access gap rather than pretending this pointer is the full implementation specification.

## Decisions to preserve

1. Reuse the existing encrypted case store, source amendments, structured state, complete active episode and candidate/audit lifecycle. Do not create a competing memory or approval database.
2. Distinguish `native_advisory`, `native_controlled` and separately authorized `api_controlled` profiles. A backend cannot intercept every ordinary ChatGPT composer message merely because it provides a tool.
3. Prefer controlled input and exact-artifact rendering within the same InnerSignal plugin. Ordinary advisory chat remains available with truthful limits. The read-only continuity MCP stays read-only; mutating application commands require their real scopes.
4. Search older raw sources in response to the incoming message, not only previously high-relevance state items. Preserve corrections, reported relationships, answered questions and the full active episode. Retain full-history input as a small-history baseline.
5. Keep reports, hypotheses, supervisor relays, drafts, translations and confirmed outgoing wording distinct. Stage model-generated state changes as proposals, not trusted evidence for their own review.
6. Bind prepared context to exact input, semantic evidence revision, source/index watermarks, guide/constitution versions, authorization and a server-recorded manifest. A digest proves identity, not comprehension.
7. Bind review to exact candidate and both writer/auditor evidence packets. Prepare reviewer evidence independently of the writer's selected snippets. Preserve accepted external provenance; do not invent a new cryptographic-attestation blocker.
8. Native automatic independent reviewer dispatch is UNVERIFIED. A same-chat follow-up, Work subagent or Codex hook is not automatically the requested native reviewer. No paid API fallback or model replacement without authorization.
9. Atomically check freshness, current authorization and review before exact release. Separate record bookkeeping from evidence revisions. Preserve bounded retries, two repair cycles, recovery and current discriminator policy.
10. Displayed, copied, operator-reported-sent and externally delivered are different states. A later edit or translation is not the approved bytes. Do not allow a technical outage to suppress immediate safety support.

## Inspected source and verification

Baseline: `bf35a23dc0146c21586108fed0f726c50db8c436`; tree `574ca1a676f1fce57dd5b36109f9d808c73ad8bb`. Reconcile current main before implementation; do not reset newer work.

Verified source weakness: `src/case-state/context-window.mjs` has `targetedRetrievalRequests(caseState, recentTurnIds)` without an incoming-message parameter and truncates state-reference requests at 24. This is a code property, not proof of any private historical incident's cause.

The current plugin path is `plugins/inner-signal-therapy/`. Its private-continuity skill explicitly describes read-only retrieval. The existing provider generation/controller and private audit remain the integration targets.

The isolated reference passed 46/46 tests on Node v22.16.0. Artifact validation passed. The inspected repository requires Node 24.18.0. No integrated repository suite, actual host flow, semantic model evaluation or clinical validation was performed by those reference checks.

## Next execution

Recommended cross-file executor: GPT-6 Astra Low; recommendation only, not an observed configuration. Work performs implementation/evidence collection, not therapeutic semantic judgment.

In parallel: verify actual host capabilities using synthetic data and implement the independent offline preparation/admission changes. Then integrate controlled native submission and exact rendering. Verify reviewer dispatch only through an approved actual interface. Follow focused/affected test scheduling and current release gates at their proper boundary.

Do not claim the no-reminder outcome complete from source storage, a green helper test, or a documented tool alone. Completion requires the actual consumer to use relevant history without an operator hint, with the selected model/billing profile and remaining limits recorded.
