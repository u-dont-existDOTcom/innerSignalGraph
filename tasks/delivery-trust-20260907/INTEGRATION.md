# Delivery-system trust lesson — 2026-09-07

Status: implementation and complete combined deterministic verification passed, owner-authorized candidate on PR #46. The immutable final head and publication/hosted receipt are recorded on the PR at closeout. No merge, installation, deployment or stable promotion.

## Authority and recovery

Direct current owner request: implement separate METHOD VALUE and DELIVERY-SYSTEM / PRACTITIONER TRUST, all listed transparency/access/sales/safety dimensions, accessible basic harm management, controller composition, seven adversarial cases and external program audit reuse. This is explicit OWNER_POLICY, not clinical efficacy evidence or a conclusion about any real practitioner's motives. Public evidence uses fictional providers and synthetic cases only; no private conversation or client identity is retained.

Fetched PR #46 branch `companion/foundations-2026-09-05` at `39fd233f31970dc3892e86a18212f541b0b1ab40`. Isolated checkout; preserve concurrent external-support task and serialize updates. PR #46 independently received readiness composition during this task, preserved through `92b1c853eeb1096c3f99b88bb4875a160a9c1d31`. This task does not merge PR #47; it composes with the actual current PR #46 implementation. Read root/state AGENTS and full current authority/read order, path-performance spec/ledger, support prototype and safeguards before edits. Existing guide sources, frozen evaluations, archived packets, independent DEV-R005 authority and release boundaries remain intact. Exact containing head belongs in final Git/PR receipt.

## Conception and reuse decision

COMPOSE / ADAPT: use the existing path controller, audited observation references, session snapshots, planner precedence and response contract. Reuse the concurrent external-support `assessFit`, `assessFinancialFit`, `assessMethod` and modular assembly, rather than creating another navigator or finance engine. The new remainder is one shared pure delivery assessment, not a clinical scoring instrument. Provider trust must not become method efficacy; reported partial relief must not become mechanism confirmation or a total cure. Preserve adverse-response precedence and accumulated prediction failures. Unknown, stale, withdrawn or replayed facts cannot certify trust. A different provider must be assessed on its own evidence.

## Active lesson contract

Universal live default-branch LESSON-INDEX routed to `patterns/task-time-lesson-activation.md`; existing path-performance primary-source scan reused for controller composition. No new clinical evidence claims, optional workflow plugin or external clinical research service.

| Trigger | Required behavior / failure condition | Enforcement / repair |
| --- | --- | --- |
| Owner trust correction | Safety paywall can be a serious delivery red flag; fail if softened to price being irrelevant or converted into motive accusation | Seven requested contrasts and literal guidance review; repair rule or language |
| Separate axes | Helpful method survives unsafe-provider finding; fail if trust rewrites benefit or relief proves cure | Pure assessment/controller tests; preserve independent outputs |
| Sensitive candidate runtime | Evidence references and current scope required; safety/consent beat benefit; no private fixture or legacy activation | Schema, audit withdrawal, lifecycle, planner tests and diff gate |
| Concurrent work | One reusable assessor wired into support audit; fail on duplicate scoring or lost commits | Fresh head readback, integration tests and changed-file review |
| Honest completion | Deterministic success is not live understanding or clinical validation | Record exact gates and unresolved semantic evaluation; no release claims |

## Implemented and reviewed

One shared pure delivery assessment (26 dimensions), method-value record and opportunity-cost policy now compose the controller and completed task-local support navigator. The case-level evidence ledger retains provider/method history across process changes while episodes select scoped keys. Runtime uses the existing controller's bounded schema-checking approach extracted into `bounded-schema.mjs`; development/provider schemas remain explicit and are tested against AJV. No dependency or lockfile changes.

Independent bounded code review found six initial defects and drove causal regression repairs: a review-tier downgrade; cross-provider evidence reuse; cross-process provider-risk reset; method-only withdrawal dropping valid delivery-risk facts; provider-only refusal marking unknown method failed; and strict provider generation-schema incompatibility. Fix verification confirmed all six repaired. A second pass found conflicting known method judgments in the external adapter and unconditional useful-benefit language; both now have explicit repairs. These are engineering findings, not domain/clinical review.

The first full checkpoint ran 897 tests: 895 passed and 2 runtime-recovery tests timed out. Isolated reproduction plus inspection identified the new Ajv import on runtime startup while the existing recovery fixtures deliberately exclude development dependencies. The repair composes a dependency-free bounded validator with the existing path validation, adds structural parity mutations against AJV and an isolated import regression, and leaves recovery tests/timeouts unchanged. Full final gates are rerun for this actual dependency repair, not to retry an unexplained green/flake boundary.

Provider generation schema requires every field with nullable optional semantics and uses supported `anyOf` branches; runtime historical snapshots retain omission compatibility. Official interface reference: [OpenAI Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs). This is static interface verification; no live provider acceptance is claimed.

## Verification receipt

On the isolated 76df72c parent, Node 24.18.0 / npm 11.16.0 final complete package gate passed: 901/901 automated tests, 29/29 graph cases, lesson/authoring validation/check/maps, immutable packet preservation, mock A001/H001, web/launch/recovery and hygiene gates. Focused affected run passed 142/142 (31 new shared assessment cases, 42 original controller, 50 original support, 15 new support composition, 4 runtime-liveness checks). Repository audit: zero errors, only the pre-existing installed-App-permission warning. Final independent bounded review verified all eight identified defects repaired and found no remaining concrete defects. No live providers ran. The generated delta contains 300 Markdown files with projection-marker changes only, plus one regenerated manifest; source graphs/guides/archives/frozen pins are unchanged.

PR #46 independently advanced with owner-authorized readiness composition during this verification. The current combined candidate preserves `92b1c853eeb1096c3f99b88bb4875a160a9c1d31` and its readiness, optional-reference and response-scope changes. The initial combined run encountered older concurrent tests still using the superseded nested readiness contract; the remote writer's corrected tests were preserved. Independent composition review then identified and repaired three causal defects (four explicit regression contrasts): nullable no-correction audit output erasing readiness, provider-only constraints poisoning method status when readiness reopens, and mismatched narrow/broad guidance in both directions. Both generation schemas now declare required nullable fields while historical runtime validation accepts omissions. The combined affected run passed 194/194, including 35 delivery assessment cases and the actual current readiness tests. The 928-test combined package below supersedes the earlier 901-test isolated result for current completion. Final immutable head and hosted links belong in the PR receipt. `CHANGED-PATHS.txt` is the exact delta from integration base `92b1c853eeb1096c3f99b88bb4875a160a9c1d31`, including regenerated provenance artifacts. Source guide text, graph nodes/edges, frozen fidelity fixtures and archived packet bytes remain unchanged.

## Combined-head compatibility repairs

The first full combined checkpoint ran 928 tests with three failures. Two reproduced the fetched readiness head's hosted failures: the fake transport had not declared the new nullable top-level readiness field, and unconditional readiness metadata changed the unrelated historical latency benchmark. The fake transport now declares the current schema, and readiness audit/response metadata is omitted when no readiness contract applies. The frozen benchmark hashes, evaluation evidence, live-call pins and production behavior for unrelated historical cases are preserved. The third failure was a test-only checksum stub exiting before consuming its input, racing the wrapper's writer under pipefail; the stub now drains input like the real checksum command. The production checksum gate is unchanged. Two fetched-head CodeQL findings on URL regexes in prompt-text assertions were initially changed to literal containment. Hosted CodeQL also flagged that form; the final test assertions parse the displayed references and compare the complete normalized URLs exactly, rejecting prefix/suffix hosts while preserving the intended reference check. The affected compatibility run then passed 56/56; the earlier broad compatibility checkpoint passed the fake-transport, publication-wrapper and URL cases but still exposed the now-repaired benchmark.

Final complete combined package: **928/928 tests**, **29/29 graph regressions**, lesson/authoring/packet/mock/runtime/smoke/hygiene gates all passed on Node 24.18.0 / npm 11.16.0. Repository audit remains zero errors with the pre-existing installed-App-permission warning. Final independent review verified the repeated-pause fix over three paused turns and confirmed that actual failed predictions still survive reopening. Narrow CI repairs were reviewed without a remaining finding. The final committed-head publication scan and hosted checks are recorded in the exact-head PR receipt; no live provider calls ran.

The final delta from the stable integration base contains 331 paths: 30 authored implementation/test/documentation paths and 301 generated provenance artifacts. All 300 generated Markdown changes are projection markers only; the other generated change is their manifest. No source guide, graph node/edge, archived packet, dependency lock or frozen evaluation fixture was changed.

## Application gate and lesson closeout

- OWNER_POLICY separation: seven requested contrast regressions, independent trace axes, and explicit clinical/motive nonclaims implemented. Price/safety/access arrangement is permitted evidence, never softened to price irrelevance.
- Safe composition: existing controller priority/consent and cumulative method predictions retained; support fit/assembly refuses high-risk delivery, while actual purchase guards retain independent quality/staging checks.
- Evidence continuity: scope, new observations, withdrawal/tombstones, provider/process transitions and contradictory method inputs tested; no private client identifiers, transcript or private-derived hashes retained.
- Fresh authority: concurrent support head preserved; exact final delta and hosted checks verified at closeout. All authoring dependency changes invalidate old proposals.
- Candidate truthfulness: deterministic/static interface evidence only. No clinical efficacy, semantic recognition, safe dose, available provider, live discovery, release or installation claim.

Project-specific lesson preserved here and in canonical handoff/workstream/specs. No universal promotion is needed. No unresolved owner policy choice blocks implementation. Candidate judgments still need actual extraction/realization and human usefulness/harm review; provider identity/currentness, proportionality, appropriate dosing/referral and alternative availability remain semantic/real-world limits. The two-month opportunity trigger is a transparent conservative implementation of the owner's several-month rule, not a validated cutoff. Runtime must not import the task-local navigator. The unmerged PR #47, independent DEV-R005 and pinned fidelity campaign remain separate; the PR #46 readiness composition is preserved and tested, not a remaining absent-module claim. No merge/deploy/stable.

## Authorized publication follow-up

After explicit owner approval, implementation commit `e5968cecccf8eb5efd15d94804129067290fdace` was pushed normally onto the preserved integration base. Exact-head hosted deterministic package passed **928/928 tests and 29/29 graph cases**, and workflow policy passed. CodeQL's analysis gate identified the two test-only URL containment assertions described above; the follow-up changes only those assertions and this receipt. Runtime behavior, source/evaluation pins and generated artifacts remain the verified implementation. Final-head check links and commit identity belong in the final delivery receipt. The prepared PR-description update was separately blocked by automatic approval review; no alternate write route was attempted.
