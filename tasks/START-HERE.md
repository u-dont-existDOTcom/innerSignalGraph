# Start here — therapy scaffold authority repair

Run `npm run task:preflight` first. It passes only on `agent/therapy-scaffold-authority-repair-20260824`.

## Objective

Compare current production control (A), advisory realization (C), and model-first formulation followed by graph audit (D) across a provenance-classified case set. Select C, D, or no change by the owner-supplied rule, then package only the selected candidate behind an immediate rollback switch.

## Immutable boundaries

- Source baseline: protected `origin/main` `a22f2e611fab778bf26b8e7215afbf85aba4ba5e`.
- Stable baseline: `origin/stable` `110ee5342e27d8f1bd3d11cc2be4d85926c255b1`.
- Experimental evidence: `exp/a001-scaffold-ablation-20260824` at `92377a2461ebd44a28d1a6a44d0348ef849d5d20`.
- Do not merge, promote, install, alter the active Guide Packet, change graph content, change hypnosis, change safety routing, or activate a production therapy default.
- Keep raw private transcripts, private ledgers, and raw private model outputs outside Git.

## Recovery order

1. Run preflight and inspect `git status --short --branch`.
2. Read `state/CODEX-CURRENT-STATE.md` and the report/checkpoint named there.
3. Inspect the private run manifest without printing private content.
4. Resume the first missing or stale stage; never repeat a valid completed provider call.
5. Verify protected refs remain unchanged before every durable checkpoint.

The final operation will be one resumable command. Until it exists, the state checkpoint names the exact next safe action.
