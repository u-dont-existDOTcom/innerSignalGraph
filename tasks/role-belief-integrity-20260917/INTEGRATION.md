# Role and belief integrity — development candidate

## Outcome and authority
Owner requested implementation after discussing unsteered AI identity drift, gradual role reversal, repeated harmful self-belief endorsement, and memory laundering. Build bounded trajectory tests through existing generation and memory paths, then targeted safeguards. Synthetic public evidence only.
Baseline: main a97a15ad6d0524916cd8e5d0e3642af17f155947. Branch: task/role-belief-integrity-20260917. Iteration lane. No merge, installation, stable promotion, deployment, real case access, paid API call, or clinical claim.

## Active lesson contract
Live Universal activation: current owner bootstrap plus task-relevant assurance, routing, test-efficiency, task-time enforcement, and delivery rules; source ad76fcac1aed29658dbdab6575f91965573e6c51.
- Preserve reports without certifying appraisals. Repetition and model interpretations are not independent evidence.
- Test actual consumers; prompt receipt is not adherence, and mock execution is not a live semantic pass.
- Preserve accountability, unfavorable facts, valid corrections, quotations, ordinary collaborative language, and technique refusal.
- Reuse provenance state, provider seams, independent audit, encrypted store, and bounded repair. No new judge or keyword truth classifier.
- Node 24.18.0, canonical test observer, isolated branch. No private data or host identifiers in Git.
- Preserve source/graphs, hypnosis consent/return, and all authorization boundaries.

## Plan
1. Reproduce automatic model-observation promotion at persistence.
2. Compose role/belief rules into actual consumers and correct that provenance seam, including legacy context.
3. Add ordinary, 50-turn self-condemnation, gradual role-reversal and contrast trajectories. Exercise generation, encrypted persistence/reopen, and seeded audit failures.
4. Focused/affected verification, final diff review, durable evidence, reviewable candidate; no deployment.

## Strategy and evidence limits
COMPOSE/ADAPT existing runtime, Node tests, and synthetic evaluation conventions; no new architecture study and no alteration to the stopped audit-architecture experiment. Preventing automatic promotion and supplying semantic boundaries closes an engineering gap; live trajectories must separately test behavioral effectiveness.
Current: implementation and complete package verification finished. Live model calls: zero.


## Verified result and disposition
The original persistence regression failed because arbitrary model-written observations were assigned `direct_report`/`high`. It now passes with an explicit model-interpretation source and retained proposed evidence. Restored legacy context is projected without that unwarranted authority; historical labels and exact source turns remain available. Source uncertainty is not a judgment about the client.

Seventeen new tests cover source handling, all named prompt consumers, the exact plugin reference, seven trajectories (89 exchanges), encrypted reopening, injected late failures, fresh repair/audit and the bounded fallback. The complete package gate passed all 1,203 tests, existing graph/authoring checks, application smoke tests, and hypnosis consent/return checks. `npm run audit:repository` passed with one existing warning: hosted GitHub App permissions are unverified. `git diff --check` passed. Machine-readable evidence and affected-source hashes are in `VALIDATION.json`; local publication scan evidence is retained in the pull request.

The initial focused run additionally exposed a test-double error: two scripted repairs had identical bytes and were correctly rejected by the existing immutable reconstruction gate. Distinct scripted revisions fixed the fixture; the intended unresolved-fallback test then passed. The final complete gate independently reran all new tests successfully.

## What remains unmeasured
No live model, private case, paid API, installed application, production service or release branch was used. Prompt activation and scripted audit findings do not establish semantic adherence, clinical usefulness or failure rates. `PROTOCOL.md` defines review of the whole trajectory, including advice premises and next-session memory; the reusable runner leaves semantic verdicts explicitly unevaluated.

## Review and continuation
This is a development candidate for the ordinary pull-request review path, not a stable release. Review the source/contract diff and the preserved contrasts. The next behavioral decision is a bounded baseline-versus-candidate live run through existing authorized provider roles; do not substitute a mock pass for that result or silently switch provider/model/spending policy. The exact executable infrastructure check is `npm run test:role-belief-integrity`. Installation and deployment require a separate authorized release.

## Lesson closeout
Project-specific causal lesson: evidence extraction can launder authority before any response is generated. Preserve source and assertion scope at persistence and at every view that can feed reasoning; test legacy retrieval as well as new writes. Evidence: the initially failing regression and final source-bound tests. Generic provenance and consumer-boundary principles already exist in Universal; no duplicate Universal rule or new global gate was added. Owner evaluation of live behavior remains outstanding. No unrelated active task, source guide, graph, or production state was changed.
