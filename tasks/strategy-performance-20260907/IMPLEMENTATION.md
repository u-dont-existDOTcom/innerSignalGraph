# Strategy-performance integration receipt

## Owner outcome and authority

Current owner request: inspect current PR/branch authority, determine whether dynamic strategy-performance evaluation and switching exist, integrate the missing architecture if warranted, and retain a de-identified client case excluding private identifying details. The personal identifier is intentionally omitted from this public receipt. The present task supplies bounded candidate implementation and GitHub handoff authority, not merge, deployment, release, client enrollment or new privacy infrastructure.

Verified starting refs:

- `main`: `64863eefa9678c063ed5c5a48a3280fa507a4c95` — development authority.
- `stable`: `110ee5342e27d8f1bd3d11cc2be4d85926c255b1` — sole installation/release authority.
- PR #46: `companion/foundations-2026-09-05`, head `aadae353dd383ac95dbe39178ee4645bab7980ec`, draft/open/unmerged.
- This slice: `therapy/strategy-performance-20260907`, a separate draft PR targeting PR #46's branch. PR #46 and its live calibration evidence are not modified.
- `tasks/ACTIVE-TASK.json` was read: the old Obsidian task is complete/nonexclusive. DEV-R005 is an independent frontier and remains unchanged.

## Result and limits

Existing current-task feedback, content-driven rerouting, partial progress, refusal and correction already exist. The missing part is an explicit expected-versus-observed strategy decision before accepted-task continuity repeats an approach. The new nullable review record, audit context, deterministic decision and planner/realization seam implement that bounded gap. No model-generated cumulative counts or clinical thresholds are introduced.

No-yield, worsening, poor fit and app rupture can trigger review without an explicit complaint. Unknown/early effects do not count as failure; improvement and mixed effects preserve useful gains. Safety and controlling external routes retain priority. A proposed alternative must be graph-eligible, and review never supplies consent to its exercise. New issues clear the old strategy review; removed evidence revokes the task through existing validation/audit machinery.

The generalized case is `docs/cases/de-identified/C-7Q4M.md`. It contains only broad concerns/resources, uncertainties and tentative options. It omits private-chat wording, source links, names, location, exact age/dates, distinctive chronology, specific substances, unusual diagnoses, and identifying metadata. It is explicitly secondhand and unverified, unimported by runtime/tests/evaluations, with no ongoing client-history or training permission inferred. No private transcript or private-derived hash was written to Git.

## Handwritten changed files relative to starting head

- `src/case-formulation/strategy-performance.mjs`
- `src/case-formulation/turn-task.mjs`
- `src/guide-graph/planner.mjs`
- `src/orchestrator/response-contract.mjs`
- `src/orchestrator/run-tiered-pipeline.mjs`
- `src/orchestrator/run-pipeline.mjs`
- `src/authoring/projection.mjs`
- `src/prompts/case-extract.mjs`
- `src/prompts/case-audit.mjs`
- `src/prompts/realize.mjs`
- `tests/strategy-performance.test.mjs`
- `tests/authoring-proposal.test.mjs`
- `docs/superpowers/specs/2026-09-07-strategy-performance-review.md`
- `docs/cases/de-identified/C-7Q4M.md`
- `docs/REASONING-SELECTION.md`
- `state/CODEX-CURRENT-STATE.md`
- `tasks/strategy-performance-20260907/IMPLEMENTATION.md`

The containing Git commit/PR supplies exact final head identity; a file cannot contain its own final commit hash.

Generated authoring artifacts are refreshed by the canonical generator because the planner and its task/review dependencies are semantic fingerprint inputs. The complete path list, including generated artifacts, is in `FILES-CHANGED.txt`. These are derivations, not guide/graph-policy edits.

## Verification

Final local package: **PASS** on Node 24.18.0 / npm 11.16.0: **754/754 automated tests**, **29/29 graph regressions**, therapy lessons, authoring validation/check/maps-check, archived packet preservation, package and all launch/smoke gates. New strategy tests: **24/24**, including real planner/prompt/enforcement seams and fake-renderer first-call/retry/block delivery. Focused authoring + strategy checks: **31/31**.

Repository audit: zero errors, one existing unrelated installed-GitHub-App-permissions warning. Precommit publication audit: **87,088 records, zero findings**; the exact committed-head recheck is reported in the PR. Independent read-only audit/test review identified and verified the Fast-mode and repeated-exercise delivery repairs and found no privacy/authority blocker in the generalized case. These are engineering/semantic artifact reviews, not clinical evaluation.

The first full gate stopped on stale generated provenance after the planner change; the generator refreshed it. The next run exposed seven proposal fixtures missing the newly fingerprinted task/review dependencies; fixture inputs were completed. The final full gate above passed without weakening checks. All **300 changed generated documents** were compared after normalizing the old/new projection marker: zero other content changes. The generated manifest additionally records the new semantic dependencies.

Final diff checks preserve guide source, graph policy/compiled graphs, packet archives, old source/fidelity pins, frozen scenarios and the prior handoff byte-for-byte. No live provider calls, clinical usefulness or model-fidelity claims are made.

## Active constraints and lesson closeout

Universal router: `u-dont-existDOTcom/universal-dev-architecture` default branch inspected at `8484f056b56b9391afd4e4357c1c18acd55aa89e`; plugin-stack PR #17 remained open, so its specified branch activation rules were used. Native tooling and one independent audit/test agent were selected because runtime integration and privacy/authority work are separable. No optional general workflow plugin was activated.

- Authority/recovery: current owner and project gates govern; verified refs and this isolated branch prevent stale-source replacement. Mechanical final diff/ref checks block unrelated edits. Existing live gate remains outstanding.
- Research-before-reinvention: architecture triggers a bounded scan; COMPOSE/ADAPT plus primary-source limits and simple baseline are in the design. Semantic review rejects novelty or clinical-efficacy claims.
- Outcome/strategy efficacy: the outcome is actual candidate response-control behavior, not document volume or passing schema alone. Deterministic planner-to-realizer tests establish the seam; live recognition/usefulness remains unmeasured.
- Task-time activation: before public handoff, inspect the literal case and diff for privacy, authority, scope, and truthful evidence. Repair any substantive failure before pushing. Do not treat scanners as proof of de-identification.

Project lesson: current-task acceptance is not evidence that continued use is helpful. Strategy review must reach both route selection and realization coverage, or a lower layer can force the paused exercise back into the answer. This is a project-specific application of the existing universal outcome/strategy pattern, so no new universal PR is warranted.

## Remaining gate

Keep draft. Evaluate the changed candidate separately with calibrated, fictional multi-turn cases; do not reuse PR #46's old source pin/results as evidence for these prompt/planner changes. Human/domain review, approved Guide Packet installation and stable promotion remain their existing gates. No clinical effectiveness, semantic detection accuracy, live fidelity completion, merge or release is claimed.

## Relational-readiness owner correction — active delta (2026-09-07)

Authority: the current owner explicitly requests adding this correction on PR #47, fetched at `d6af04da5564564d62fe17e0a096eca2386fe311`. A person need not be fully healed for romance, but current foreseeable serious relational harm requires a practical readiness gate before dating is normalized or recommended. Ordinary support is separate and remains available. The error was collapsing human connection and romance into one variable and failing to condition on instability/risk. This is an owner policy correction, not a claim of clinical efficacy. Implement only this candidate delta; no merge, deployment, stable promotion or private-case expansion.

Active lesson application contract:
- Owner correction / mechanical + semantic: structured current evidence, explainable decision, revisitable markers, substitution/adverse trajectory control and response contract; regressions must reject risky endorsement and permanent diagnosis-based bans. Repair any bypass before handoff.
- Authority/privacy / mechanical + semantic: use the fetched PR #47 head; preserve candidate/owner gates, existing private-data boundaries, guide sources, packet archives and frozen fidelity artifacts. Use fictional tests only; do not import private conversation or client details.
- Recovery / mechanical: update this existing receipt and canonical state; final Git head and delta path list identify the result without a second recovery ledger.
- Verification / mechanical: focused regressions in the implementation loop, one full final deterministic package checkpoint, exact-head audits. Timing observer is external to the repository and records under `.git`; no redundant full green reruns.

Result: the candidate now has `turn_task.relational_readiness`, evidence validation, deterministic `NOT_BLOCKED` / `ASSESS_BEFORE_ROMANCE` / `PAUSE_ROMANCE` decisions, explainable reasons/readiness markers and independent execution-contract trace. Strategy review rejects instrumental support success and worsening dependency. Initial dating requests are audited even on Fast; current protection remains first. Dating cues are suppressed when pursuit is paused, unresolved readiness can ask one bounded assessment question, and the renderer receives a required advice declaration with one corrective retry before blocking repeated violations.

Independent read-only review found and verified repairs for fresh new-topic assessments being erased (including initially unknown readiness), downstream resurrection of an intentionally empty dating question, and rejection of legitimate partial friendship gains. Separate partial-gain acknowledgment now preserves useful friendship evidence without granting support-goal success to instrumental pursuit. These are engineering/semantic artifact reviews, not clinical evaluation.

Final deterministic validation: **778/778 automated tests and 29/29 graph regressions** passed in `npm run verify` on Node 24.18.0 / npm 11.16.0. The package also passed therapy lessons, authoring validation/check/maps, archived packet preservation, syntax, launch/mock/smoke and hygiene gates. Focused strategy/readiness tests: **48/48**, including **24 new relational cases**; affected authoring tests: **7/7**. The earlier integration checkpoint also passed existing deep/forensic/tiered pipeline tests. First focused run had two fixture mistakes (external-route task identity and missing prompt context); both were repaired. No live provider calls ran.

Repository audit: zero errors, one existing installed-GitHub-App-permissions warning. Fetched-baseline publication audit passed with 88,056 records and zero findings. The new committed-head audit and immutable final head are recorded on the PR after readback. Prior 754-test and hosted evidence above applies only to the fetched baseline, not this delta.

Diff/preservation: **300 generated documents** differ only by the reviewed projection-input marker. The generated manifest adds the relational-readiness semantic dependency. Guides, graph candidates/compiled policy, packet archives, prior case abstraction, source/fidelity pins, frozen artifacts and the prior PR #46 handoff are byte-preserved. No private conversation, identifier, identifying case detail or private-derived hash was added. The complete delta path list, including generated files, is `RELATIONAL-READINESS-FILES-CHANGED.txt` in this task directory.

Task-time admission: PASS for owner-correction behavior (the five required scenarios plus the adversarial seams above), authority/privacy (literal diff and protected-path checks), recovery (this receipt and canonical current-state routing), and deterministic verification. The final implementation head is the containing Git commit; it cannot be embedded into its own content.

Measured execution at the verification checkpoint: 879.59 seconds task elapsed, 149.12 seconds tests (16.95%); focused 5 runs / 4.08 seconds / one failure-discovering run; affected 1 / 17.28 seconds; full 1 / 127.76 seconds. Mutation runs and forced redundant-green reruns: zero. No identical full green rerun was executed. The independent reviewer separately ran five targeted cases, all passing.

Unresolved semantics and next gate: current clinical stability/harm, temporal freshness, thresholds and helpful readiness markers remain audited case judgments rather than a validated clinical scale. The extractor can omit/misclassify an assessment and the renderer can mislabel prose; declared-action/quote checks do not establish free-text semantic safety, model detection accuracy, fidelity or efficacy. Validate separately with calibrated fictional multi-turn live cases and human/domain review. Preserve the existing packet owner decision/install and stable release boundaries; keep PR #47 draft. No merge, deployment or promotion is authorized. This project-specific correction is captured in the design and reasoning supplement, so no universal-policy publication is needed.
