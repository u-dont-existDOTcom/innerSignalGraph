# R1-R6 candidate implementation receipt

Owner direction: 2026-09-06, continue the specific approved reconciliation and ensure actual responses are tested against the guide, including whether the map is too crude. This is authorization for development-map/runtime changes and synthetic evaluation, not release, stable promotion, clinical efficacy or new privacy/storage policy.

Baseline public source: `77bd76e2b8a5252dedfcaa502673dd877b1336ae` (prior code `046614b045d4a15ea71b3b61e74b11d6b615a2ed`). Local work uses the complete Git history and exact Node24.18.0/dependencies retrieved in a least-privilege GitHub Actions artifact. npm locally is10.9.2. Temporary source-transfer workflow is removed at closeout. No model credentials or real client data are involved in that transfer.

## Implemented changes

R1: current-issue relational completion, separation of live material from distinct repetitive checking, planner-derived leave-alone eligibility, and a present protector's willing/blocking stance. Topic changes and withdrawn observations conservatively invalidate completion/permission. Unknown is not permission. No hidden child/material is inferred from symptoms.

R2: safety-compatible question selection; no repeated inward-effect question once it is known; an explicit no-question contract cannot fall through to stale adjudication. Approved task questions use missing information rather than arbitrary renderer replacements.

R3: optional versioned turn_task in the existing snapshot/plan, strict provider schema and local validation, observation binding, correction/invalidation, phase/consent/current response, explicit current-turn required nodes separated from contextual diagram nodes, and app-authored task guidance/success signals delivered to the realizer. No separate long-term memory service. Unknown-only capacity no longer triggers missing-capacity exercises. Existing graph packets without taskPolicyVersion retain legacy semantics.

R4: existing outward/Protector/apprentice guidance now plans and reviews actual attempts: feasible cue/size/resources, function and consequences, not automatic resistance or productivity. Rest and interdependence remain legitimate.

R5: existing inward/care routes carry current emotional task, response/fit and partial change points, adapt intrusive care, preserve proportionate anger/grief and valid correction, and include delight/exploration rather than care as duty alone. No automatic chairwork or memory discovery. Emotion-focused work is explicitly distinct from tapping's EFT acronym.

R6: one new IC.SPIRITUAL_STRUGGLE node for sacred loss or pain within a spiritual relationship. Borrowed spiritual love remains available; continued devotion and appropriate relinquishment do not mean failed agency. No supernatural cause or clinical effect is certified.

Candidate totals:53 nodes,70 edges,80 source sections,26 amendments,29 graph regressions. G026-G035 are new; existing G001-G018/G025 and frozen behavioral v1/v2 cases remain unchanged. SourceRefs use four explicit new owner amendments rather than unresolved book IDs. Generated projection includes292 files.

## Testing the map rather than only compliance with it

New guide-fidelity suite:12 original development scenes,23 user turns,three conditions (normal candidate pipeline, same responder with full guide/no graph, normal pipeline with source-augmented realization),two independent blinded graders, six positive/negative grader controls, source/quote validation and refusal to average away uncertainty/blockers. No live defaults or fallback model. Source/runtime/config hashes, call reservations, resumed completed requests and detailed raw-versus-enforced traces are retained. It is not a held-out or clinical trial.

Current blocker: authenticated exact responder/grader configuration is absent in this supervisor environment. Preflight therefore records0 real responder outputs/independent grades. A successful real API identity probe is mandatory before scoring; xhigh is explicitly requested but hidden internal effort cannot be independently measured. User direction remains GPT-5.6 Sol xhigh; neither labels nor this document prove entitlement.

## Verification record and exclusions

Actual full-baseline planner probes ran locally under Node24.18.0, confirming the earlier source-level findings. Initial full package test reached706/708 passes; two current-policy assertions needed an explicit v5 contract/fingerprint update. The historical latency evidence and frozen model fixtures were not altered; the two/three mock-stage latency shape and one-pass planning remain preserved. The final verification log and commit association are recorded in the closing checkpoint/PR comment, not invented here before the run completes.

No model inference, clinical efficacy, author endorsement, release, installation or merge is implied by deterministic pass counts. The whole-source reference can also fail; compare case-level source judgments and actual trace evidence before attributing a failure to extraction, representation, delivery, realization or enforcement. Detailed source locations remain in THERAPEUTIC-SOURCES and the reconciliation specification; no repeated broad reading is needed.

## Recovered-work verification and review fixes

The transferred 54-file patch was recovered from the generated code-only archive and every file was checked against its retained SHA-256 manifest before execution. A fresh Node24.18.0 run passed all708 initial unit tests; its package smoke exposed one stale 52-node assertion in scripts/verify-package.sh, now explicitly updated for the approved53-node candidate. No old latency timing threshold or frozen behavioral input changed.

Further review added a regression that imminent danger remains protection-first even when another field calls the location safe, and a grader-contract regression that uncertainty cannot downgrade a serious blocker. The entire three-arm smoke/replay is also exercised through fake transport, including the actual case/planner/realizer/enforcement path and independent-review plumbing. Those fixture outputs are not actual bot responses or semantic-quality evidence.

The source-augmentation comparison uses the same upstream implementation but separate stochastic histories/extractions. Differences are diagnostic clues, not isolated causal estimates; saved-plan replay is needed before attributing a difference solely to additional source context.
