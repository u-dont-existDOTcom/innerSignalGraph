# InnerSignal wisdom practices — bounded Work execution

**Repository:** `u-dont-existDOTcom/innerSignalGraph`
**Task:** `wisdom-practices-20260912`
**Target:** a fresh isolated task branch based on current development `main`
**Reasoning:** LOW for the supplied deterministic integration. Chat retains all therapeutic, architecture and source judgments.
**Scope:** executable graph candidate + source/provenance + actual runtime wiring + synthetic tests. No install/deploy/stable promotion or paid model use.

## 0. Read and admit the exact task

Load the live default-branch `u-dont-existDOTcom/universal-dev-architecture/AGENTS.md`, follow the live authority order and task-relevant rules, including test-efficiency and Chat/Work routing. Then load the project's live AGENTS and normal authority order. Begin the new chat's first final message with a real date/time/timezone.

This task is independent of old private-case tasks and their completed candidate audits. Do not resume, fetch, audit, edit or publish any real private case. Do not treat an unrelated old global BLOCKED/task label as this task's blocker. Preserve all existing work in other branches/worktrees.

Read these packet files in order: `notes/OWNER-OUTCOME.md`, `BASIS.json`, `notes/SEMANTIC-DECISIONS.md`, `notes/EXACT-CODE-INTEGRATION.md`, then the payload and acceptance definitions. The attached source draft and conception are background authority; do not conduct new research or re-author the method.

## 1. Establish a clean, exact base

Use the existing authenticated checkout or GitHub CLI; do not ask the owner to gather routine logs. Fetch current main and create an isolated branch/worktree named `task/wisdom-practices-20260912` (use a safe unique suffix only if an unrelated branch already owns that name). Do not reset, clean, stash or overwrite someone else's work.

Run `python3 prepare_candidate.py --repo /absolute/path/to/checkout --output /absolute/path/to/new-staging-directory` from this packet. It is read-only toward the repository and checks the critical live Git blob hashes against BASIS.json before producing exact candidate graph/source members. The recorded tree ID is a tree, not a commit; record the actual current base commit separately.

If a bound file changed, first determine whether the exact same change is already present. An exact already-integrated payload can be verified and resumed. Otherwise stop only the conflicting edit and send Chat the file-level diff and packet manifest. Do not mechanically overwrite newer semantic work or redesign around it. Unrelated new commits do not invalidate unchanged bound files.

Use the repository's supported Node 24 line and npm version from its live declarations. This packet does not authorize dependency upgrades. Run `npm ci --ignore-scripts`. Start the existing test-cost observer or the current canonical universal observer before repeated tests. Focused/affected checks are the inner loop; no repeated unchanged full suite.

## 2. Prepare the exact source and code additions

Use the generated staged source/amendment members, full graph records, existing-node append operations and graph cases. Do not summarize them or substitute prose-only documentation. Install the supplied pure helper and apply `notes/EXACT-CODE-INTEGRATION.md` mechanically. All old graph node fields not explicitly appended remain unchanged; no graph deletions, changed gate priorities or global rearrangements are allowed.

The owner has approved the described practices. Record the de-identified current owner-request/source provenance for the supplied amendments; do not infer clinical validation or exact Guide Packet approval from that approval. Preserve prior amendment items and their status. Do not alter canonical article HTML/text. Preserve the full source draft under the task's research record; no private family/source names or identifiers.

Use the existing authoring proposal and Guide Packet mechanism, not a hand-edited Mermaid/Canvas or a forged approved flag. Prepare source/contract support on the isolated task branch as permitted by the current source workflow; regenerate the read-only projection needed as proposal base. The graph semantic additions are the exact staged records from this packet. Generate a full-record proposal, regression coverage and owner decision cards with the existing schema/CLI. The known commands are:

```bash
npm run authoring:project
npm run authoring:proposal:new -- --id wisdom-practices-20260912 --node IC.BEST_FRIEND_PERSPECTIVE --node IC.BORROW_ONE_FUNCTION --node IC.BORROW_LOVE --node IC.PROTECTOR_ACTION --node ROUTE.ACT_OUTWARD --node ROUTE.RELATIONAL_REALITY_CHECK
npm run authoring:proposal:build -- --id wisdom-practices-20260912
npm run authoring:proposal:check -- --id wisdom-practices-20260912
```

Use the live CLI's declared list syntax if it differs from repeated `--node`; this is only argument serialization, not permission to change the selected nodes. Add the eight supplied node records and twelve supplied edge records as full-record additions in the existing proposal language. Add the G930–G962 cases, checking there is no ID collision. If those IDs now exist, choose the next contiguous unused numeric G range and update only the fixture index and references, not expected behavior. The materializer emits a receipt with exact source/graph hashes; bind that receipt to the task ledger.

Unknown proposal fields or parser conventions must be handled using the current schema/example, not guessed. Mechanical serialization changes are allowed; graph behavior, source wording and expectations are frozen by this packet. No new authoring engine or independent approval store.

## 3. Wire real execution and the return-to-care continuation

Apply the exact derived-variable and bounded outward-support integration. Load the extraction/audit additions only when the actual candidate graph bundle contains all eight nodes. Reuse existing grounded `turn_task`, observation withdrawal, issue reconciliation, path-performance controller and encrypted continuity. Do not introduce a new task queue, database, scheduler, send capability or private-draft persistence feature.

Ensure that the new graph conditions can actually match through the production extraction → case audit → planner → realization path, not merely in a diagram or a test that passes node IDs by hand. A fitting offer remains optional; an explicitly requested method already supplies consent. Current safety, actual outward action and the current relational reality check retain precedence. The editor can be required support for an accepted outward communication task without displacing that primary job.

The actual reported outward action and the later inward return must remain distinct in task state. The request “come back to my hurt” is not complete because a message was sent. Test partial response, deferral/refusal/close, changed issue and withdrawn evidence. No compulsory reparenting, invented child-state or stale task revival.

## 4. Run the supplied and existing checks

Run the standalone checks first:

```bash
python3 checks/check_packet.py
python3 -m unittest discover -s checks -p 'test_staging.py' -v
node --test checks/perspective-practices.test.mjs
```

Adapt the helper test import to `../src/guide-graph/perspective-practices.mjs` when copying it into repository tests. Do not replace production integration tests with that standalone helper test.

Run all 33 supplied graph fixtures against the actual compiled candidate using `runGraphRegressionSuite({ root, bundle: candidateBundle, cases: suppliedCases })`; the existing runner was inspected and accepts those parameters. Once the candidate is legitimately reconciled, add cases to `corpus/graph-cases/` and use the normal `npm run graph:test` entry point too. Preserve every pre-existing regression.

Implement and run the synthetic multi-turn and negative checks in `notes/BEHAVIORAL-ACCEPTANCE.md` through the existing test harness. These are prescribed expectations; do not invent a clinical evaluation or give yourself a semantic approval based on string presence.

Required integration checks, at the appropriate candidate/reconciliation boundary:

```bash
npm run graph:compile
npm run graph:test
npm run therapy-lessons:verify
npm run authoring:validate
npm run authoring:check
npm run authoring:maps:check
npm run audit:repository
npm run audit:publication
```

Run focused new and affected Node tests, including extraction/audit, ordinary/private response, continuity and legacy packet compatibility. At the graph reconciliation / completed PR-candidate boundary run the complete `npm run verify` once. The existing reconciliation command itself runs the complete gate; do not repeat it unchanged. Do not rebuild or edit immutable r01/r02 ZIPs to hide a regression. Do not weaken an existing assertion or change expected therapy behavior to make a test pass.

If a failure is a typo, missing import, output-path/CLI serialization issue, deterministic generation drift or a directly expected count change, repair it and rerun the affected checks. If it would require changing therapy semantics, precedence, source attribution, privacy, approval rules or the frozen expectations, return the exact failure to Chat. Do not silently increase reasoning or start a paid/model experiment.

## 5. Respect exact approval, then reconcile through the existing mechanism

Do not forge `guide-owner-decisions-v1`, assign yourself the owner's identity, mark `allApproved`, invent an approval hash, or replace the required exact packet approval with a Markdown claim. The existing lifecycle owns that boundary.

If the current owner authorization can be bound through the existing mechanism to the exact generated decision cards without adding or changing a substantive choice, use that supported mechanism and preserve its source/decision evidence. If the mechanism requires a fresh exact artifact approval that is not available, preserve the complete compiled/tested proposal and candidate packet, report `BLOCKED_EXACT_PACKET_APPROVAL`, and deliver the actual approval-ready artifact and concrete decision cards to Chat. Do not declare the graph integrated and do not bypass reconciliation by copying the staged candidate into an authoritative path.

With a legitimately approved exact packet, use:

```bash
npm run authoring:proposal:reconcile -- --id wisdom-practices-20260912 --packet-id <ACTUAL_PACKET_ID> --packet <ACTUAL_APPROVED_PACKET_PATH> --sha256 <ACTUAL_APPROVED_PACKET_SHA256>
```

These three values must come from the generated and genuinely approved artifact, not invented constants. Reconciliation is task-branch-only, verifies the full graph/source/approval chain, regenerates maps and runs the package gate. It never installs or writes stable. Keep graph-generated files generated. Preserve historical migration records; any forward link from old documentation-only common-humanity proposals must truthfully point to the new executable candidate without rewriting old owner decisions.

## 6. Deliver a reviewable result, not a claim

Commit only this task's safe source/code/proposal/candidate/generated/test/receipt files to its isolated branch. Open one PR against main with a plain-language summary and exact validation receipt, preserving the distinction between pending proposal, reconciled development graph, merge and installed policy. Keep the PR unmerged for Chat review unless separately authorized through the current governance. No stable promotion, installation, deployment, real-case operation, public case text, new provider or paid call.

Update the task-local recovery ledger and project checkpoint with an accurately scoped entry; do not replace the entire existing project state. Complete task-local lesson closeout at the actual completion level. Do not edit root governance merely to add this feature; changing protected governance requires its separate integrity bindings.

Return: current base/head; changed paths; proposal ID; generated packet ID/SHA and actual approval state; graph/source/projection hashes; counts and results of focused/regression/full checks; private/legacy non-effect evidence; remaining exact blocker if any; and a direct downloadable ZIP of the candidate/receipt. Labels must distinguish PREPARED, COMPILED, TESTED, APPROVAL_PENDING, RECONCILED, MERGED and INSTALLED. Only completed transitions may be reported. Model adherence and human efficacy remain NOT ESTABLISHED by these engineering tests.
